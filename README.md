# largest-number-c
A simple C program to find the greatest of two numbers using if-else statements.


Largest Number in C

This is a simple C program that finds the greatest of two numbers using if-else conditions.

 Features
Takes two integer inputs from the user
Compares both numbers
Displays the greatest number or if both are equal
